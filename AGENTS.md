# 项目协作约定

- 新增或更新的说明文档使用中文；代码标识符、命令、路径和官方字段名称保留原样。
- Git 提交标题与说明使用中文。只有用户要求提交时才创建提交，不自动推送远程。
- 修改聊天界面时保留中继直连、云端配对、审批、会话历史、任务停止和占用处理能力。
- 网页依赖本地托管，不从第三方 CDN 加载，以减少远程访问依赖。
- 日常验证仅运行本次新增、修改及直接受影响的测试，可通过指定测试文件或 --test-name-pattern 缩小范围；不每次重复运行无关的旧测试、全量浏览器回归或全部屏幕宽度。涉及共享接口时补测相关调用方，只有发布验收或用户要求时才运行全量测试，并说明实际验证范围。
