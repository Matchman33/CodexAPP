# 阶段记录与 TODO

## 当前暂停节点

2026-09-12：保存已有界面、思考等级和连接恢复改进，暂停在「无域名 frp 部署骨架已建立，完整配置生成器尚未实现」节点。

服务器目标为 `39.102.80.25`。尚未登录服务器、安装服务、申请证书或调整安全组；以下骨架不能直接作为完整部署包运行。

已建立 `deploy/frp/stack.example.json`、服务端 Dockerfile 及本地部署配置忽略规则。frp 固定为 0.71.0，Dockerfile 预留官方压缩包的 SHA-256 校验；构建参数将由后续生成器提供。未实现的 npm 配置生成器入口暂不注册，避免留下失效命令。

## 已保存的改进

- 动态模型与思考等级选择，支持中继、云端 Agent、网页和 Expo 客户端。
- 手机版聊天界面、会话搜索、安全 Markdown、本地托管图标和网页依赖。
- Tailscale Serve 使用说明、登录按钮居中、首次中继入口识别。
- 网页连接超时、单次退避重试、旧连接隔离、切网和前台恢复重连。
- 中文文档及中文 Git 提交约定、后续改进清单。

## TODO：无域名、多电脑 frp 部署

- [ ] 实现 `scripts/frp-config.mjs`，生成配置并校验电脑编号、端口冲突、地址及随机隧道密钥；完成后再添加 npm 命令。
- [ ] 生成 Docker Compose、frps、HTTPS 网关配置；代理端口仅在容器网络内可见，不向公网发布。
- [ ] 配置 frp 服务端证书与电脑端 CA 验证，保护服务器到电脑的隧道；隧道密钥与应用 Token 分开管理。
- [ ] 实现公网 IP 证书申请、HTTP-01 验证入口、定时自动续期与网关证书重新加载；确认选定 Certbot 镜像可用。
- [ ] 实现服务器安装、状态检查及故障排查脚本；端口冲突时退出，不关闭已有服务或防火墙。
- [ ] 实现 Windows frpc 下载、摘要校验、启动和当前用户登录自启；不复制 Codex 登录凭据。
- [ ] 完成多电脑与迁移说明：第一台使用 HTTPS 8443，新增电脑分配独立入口；迁移同一入口前停止旧电脑的对应隧道。
- [ ] 补充服务器安全组、Token 防泄漏、配置权限、续期监控与服务商公网 IP 网站接入要求。
- [ ] 取得服务器 SSH 登录方式，确认操作系统、架构及现有端口占用，再经授权执行实际部署。
- [ ] 部署后使用手机外网验证 HTTPS 信任、WebSocket、运行状态恢复和停止任务，以及多电脑隔离；验证过程中不自动发送真实 Codex 提示词。

## 验证与协作约束

按用户要求，当前及后续默认不运行单元测试，不打包 iOS / Android。已有网页改进做过语法和隔离浏览器检查；本阶段 frp 骨架尚未构建或联调，不能视作已部署。未自动推送远程仓库。

`deploy/frp/runtime/`、本机运行配置、密钥和证书不提交 Git。`cloudflared.exe` 与 `%SystemDrive%/` 不属于本次源码检查点，保持原样且不纳入提交。
